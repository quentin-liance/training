# Streamlit secrets configuration for production
# Add any secrets here if needed in the future

# Example for database connection:
# [database]
# host = "your-database-host"
# username = "your-username"
# password = "your-password"

# Example for API keys:
# [api]
# key = "your-api-key"
